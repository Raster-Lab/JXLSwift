# JXLSwift — Status Report (May 2026)

**Project:** JXLSwift — pure-Swift JPEG XL implementation
**Branch:** `main`
**Reporting period:** v0.5.0 → v0.9.0 (in progress)
**Date:** 2026-05-04
**Repository:** https://github.com/Raster-Lab/JXLSwift

---

## 1. Executive summary

JXLSwift is a ground-up, independent implementation of the JPEG XL Image Coding System (ISO/IEC 18181) in **100 % pure Swift 6.2 with strict concurrency**. The goal is a Swift-native codec for the DICOMkit ecosystem with no C / Objective-C / runtime dependency on libjxl.

In the reporting period, the project advanced from "VarDCT primitive math complete" (v0.5.0) to "every AC strategy in the cjxl quality sweep decodes end-to-end on Apple Silicon with hardware acceleration" (v0.8.0), and is now in a focused **pixel byte-equality** push (v0.9.0) to close the residual drift between our decoder and the libjxl reference.

**Headline progress:**

- **350 tests passing, 0 failures** across the full Swift test suite.
- **All 5 cjxl-distance test fixtures (d=0.5 / 1.0 / 2.0 / 5.0 / 10) now decode end-to-end** through our pure-Swift VarDCT pipeline.
- **Apple Silicon UMA acceleration shipped** — vDSP-backed `AccelerateDCT` provides ~4.5× IDCT speedup, scalar Swift remains the source of truth.
- **Pixel byte-equality bug localised** to AC contribution — uniform-colour blocks are byte-equal to djxl within ±1 byte; gradient blocks expose a quantizer-scaling discrepancy that is now well characterised and ranked for next bites.

---

## 2. What was delivered

### v0.5.0 — VarDCT decode (single-group, no restoration)

**Demo:** "JPEG XL VarDCT decoded in 100 % Swift."

Full pipeline through the cjxl-d=1 8×8 fixture: AC token stream → dequant DC + AC → 8×8 IDCT → CFL → inverse OpsinXYB → sRGB OETF → 8-bit RGB. Per-channel means within ±20 of `djxl 0.11.2` reference.

### v0.6.0 — Restoration filters

Gaborish 3×3 separable smoothing and the EPF (edge-preserving filter) framework with EPF1 and EPF2 kernels. Both wired into the decode pipeline and gated by frame-header flags.

### v0.7.x — Multi-block, multi-AC-group, EPF kernels

8×8 / 16×16 / 32×32 fixtures + 300×300 multi-AC-group solid-grey fixture all round-trip. Per-block QF, coefficient-level CFL, multi-AC-group TOC-driven section seeking with per-group rANS state. v0.7.1 added Lehmer-code coefficient-order skipping and proper multi-cluster `blockCtx` routing.

### v0.8.0 — Multi-AC-strategy + UMA backend (shipped)

**Demo:** "Every cjxl distance level (0.5 → 10) decodes end-to-end with hardware-accelerated IDCT."

- Per-strategy IDCT for **DCT8x8, DCT16x16, DCT8x16/16x8, DCT32x16/16x32, DCT32x32, DCT64x64, DCT64x32/32x64**. Every AC strategy used by the SWEEP test fixtures decodes cleanly.
- **`AccelerateDCT`** — Apple Silicon UMA backend via `vDSP_mmul`. **~4.5× IDCT speedup** measured on 8×8 (5000 iters: scalar 14.6 ms → UMA 3.2 ms). Falls through to the scalar `LibjxlIDCT` reference on non-Apple platforms.
- **`LibjxlIDCT` / `LibjxlDCT`** — matrix-vector port of libjxl's reference IDCT, replacing the orthonormal `DCT2D` plus per-coefficient bridge factor used previously.
- **`ACStrategyImage`**, **`CoeffOrders`** (natural order + Lehmer-code permutation decode), **`LowestFrequenciesFromDC`** extensions, **EPF0** (12-neighbour 5×5-plus bilateral filter).
- Critical correctness fixes: inverted `prev` flag in AC decode (was breaking d=0.5 SWEEP), per-cell QF stamping, per-channel `x_dm_multiplier` / `b_dm_multiplier`, channel iteration order corrected to libjxl storage `{1, 0, 2}`.
- 345 tests passing.

### v0.9.0 — Pixel byte-equality push (in progress)

The headline goal is closing the residual textured-fixture pixel drift between our decoder and `djxl 0.11.2`. Six sub-bites have shipped this period:

| Sub-bite | Outcome | Status |
|---|---|---|
| **v0.9.0a/b** — `kInvDCQuant` indexing audit + AFV foundation | XYB-c indexing (`X=0, Y=1, B=2 → 4096, 512, 256`) confirmed correct. AFV 16×16 frozen basis matrix `k4x4AFVBasis` + `idct4x4` primitive ported from libjxl. | ✅ shipped |
| **v0.9.0c** — CFL slope investigation + investigation document | CFL slope formula, per-tile lookup, and constants (`kBaseCorrelationX = 0`, `kBaseCorrelationB = 1`, `kDefaultColorFactor = 84`) all match libjxl byte-exact. AC-CFL contribution too small to matter at current scale. Investigation doc captures the full state. | ✅ shipped |
| **v0.9.0d** — `AdjustQuantBias` per libjxl | Implemented `quantizer-inl.h::AdjustQuantBias` (`q == 0 → 0`; `\|q\| == 1 → ±0.5`; `\|q\| ≥ 2 → q − 0.145/q`). Wired into all 7 AC dequant call sites. Net pixel impact ±0.03 mean per channel — correct but small. | ✅ shipped |
| **v0.9.0e** — uniform-block diagnostic | **Critical localisation.** Three uniform-colour 8×8 blocks (red, grey, blue-tinted) byte-equal to djxl within **±1 byte per channel** → DC + OpsinXYB pipeline confirmed correct in isolation. | ✅ shipped |
| **v0.9.0f** — gradient-block diagnostic | Single-axis gradient 8×8 blocks expose the AC drift signature precisely. With current `×64` seed scaling, AC contribution is zero (gradient washed out to DC mean). With `×64` removed, AC overcorrects to ~1.286× the correct magnitude. **Bug now characterised, not just observed.** | ✅ shipped |
| **AFV full overlay, DCT128/256, Metal compute, lossy encoder** | Foundation infrastructure for all four is in place. | ⏳ deferred to follow-up bites |

---

## 3. Test suite

| Metric | Value |
|---|---|
| Total tests | **350** |
| Passing | **350** |
| Failing | 0 |
| Skipped (cjxl/djxl unavailable, etc.) | 3 |
| Total run time (release config, Apple Silicon) | ~21.5 s |
| Build time (release config) | ~21 s |

The test suite covers every spec layer the codec implements: bitstream primitives (BitReader / BitWriter / U32 / U64 / Enum), foundation (signature, SizeHeader, ImageMetadata), entropy (HybridUint, prefix codes, rANS, ANS distributions, context maps, LZ77 hybrid header), Modular sub-codec (predictors, RCT, Squeeze, weighted predictor, MA-tree, full per-pixel decode driver), VarDCT (DCT primitives, AC strategies, CFL, dequant, IDCT, OpsinXYB inverse), restoration (Gaborish, EPF1, EPF2, EPF0), and end-to-end cross-validation against `cjxl` / `djxl` 0.11.2.

### Quality benchmarks (informational, not pinned)

**SWEEP byte-diff vs `djxl 0.11.2`** — 64×64 textured PPM, cjxl-encoded at distance d:

| Distance | Max byte-diff (R, G, B) | Mean byte-diff (R, G, B) |
|---|---|---|
| d=0.5 | (26, 20, 42) | (6.09, 6.41, 7.57) |
| d=1.0 | (40, 22, 115) | (4.97, 4.74, 11.38) |
| d=2.0 | (32, 24, 63) | (4.09, 4.18, 9.75) |
| d=5.0 | (25, 18, 65) | (4.31, 3.02, 8.49) |
| d=10 | (12, 25, 57) | (3.13, 3.81, 5.50) |

**Uniform-block byte-diff vs djxl** — three uniform-colour 8×8 blocks at cjxl-d=1: **±1 byte per channel** on every sample. Confirms the DC + OpsinXYB pipeline is correct.

---

## 4. Architecture and design constraints (re-affirmed)

The hard project constraints stated in the original spec are unchanged and continue to be honoured:

1. **Pure Swift 6.2** — no C / C++ / Objective-C in the runtime. The only acceptable foreign code is at testing time (where we shell out to `cjxl` / `djxl` / `jxlinfo` for cross-validation).
2. **Strict concurrency complete** — every public API is `Sendable` where applicable. No `nonisolated(unsafe)` mutable state in `Sources/JXLSwift/`.
3. **No shared mutable global state** — actors for shared mutability; explicit dependency passing otherwise.
4. **libjxl is a test-only oracle** — never a runtime dependency, never a fallback codec backend. The `libjxl-backend` branch preserves the historical Swift-wrapper trajectory for reference only.
5. **Not DICOM-aware** — JXLSwift is the codec; DICOM file format / metadata / transfer syntax handling lives in DICOMkit.

Performance priorities, in order: **speed → compression → resource efficiency**. `EncodingOptions` exposes configurable trade-offs.

Platform targets: **macOS / arm64 (primary)**, macOS / x86_64 (modular), Linux / x86_64 (modular). Platform-specific code paths (e.g., `AccelerateDCT`'s vDSP path) live behind a clean abstraction so they can be deleted later without disturbing the core. The scalar Swift path is always the source of truth.

---

## 5. Pixel-accuracy investigation (v0.9.0)

This is the active engineering focus and the dominant reason v0.9.0 is not yet shipped.

### Status

**Localised** — the residual pixel drift is in the **AC contribution to VarDCT decode**, specifically in the dequant→IDCT bridge or AC channel mapping. The DC + OpsinXYB pipeline is confirmed correct in isolation.

### How we know

| Diagnostic | Result | Implication |
|---|---|---|
| `testVarDCT_UniformBlock_DjxlByteDiff` (red, grey, blue-tinted uniform 8×8 blocks at cjxl-d=1) | **±1 byte per channel** on every sample | DC dequant + DC-CFL + OpsinXYB inverse + sRGB OETF are all correct in isolation. |
| `testVarDCT_GradientBlock_DjxlByteDiff` (horizontal R = 100 + x·16 ramp, cjxl-d=1) | With current `×64` seed scaling: pixel 0 = 156 (= DC mean) → AC = 0 | AC contribution washes out entirely → bug is in AC dequant. |
| Same gradient with `×64` removed | Pixel 0 = 84 → AC contribution = 72 (correct = 56) | Removing `×64` is closer but overcorrects by ~1.286× → bug isn't a clean one-factor toggle. |

### Confirmed correct (do not re-investigate)

- IDCT (matrix-vector port of libjxl `IDCTSlow`, byte-equal to reference within float epsilon; `AccelerateDCT` byte-equal to scalar reference across all square + asymmetric shapes).
- OpsinXYB inverse matrix and bias constants (verified byte-equal against libjxl `cms/opsin_params.h`).
- OpsinXYB inverse formula.
- `kInvDCQuant` indexing convention (XYB-c).
- CFL pipeline structure (DC-CFL at DC plane; AC-CFL at coefficient level).
- CFL slope formula (`YtoXRatio(slope) = base_correlation_x + slope / color_factor`).
- Default colour-correlation constants.
- Per-cell QF stamping for multi-block dequant.
- AC `prev` flag.
- Channel iteration order (`{1, 0, 2}` storage).

### Open suspects (ranked, with rationale)

1. **Quant matrix scaling chain.** The `scaledForBitstream(×64)` seed multiplier and the LIBRARY-mode vs DCT-mode quant matrix loading paths interact in a non-clean way. Two hard data points captured (current `×64` → AC = 0; no `×64` → AC overcorrected by 28 %). The actual fix likely involves a second scaling factor we haven't located.
2. **Channel-order mismatch in the per-channel dequant matrices.** Our `(x, y, b)` tuple labelling vs libjxl's internal `DCT()` channel order. To be verified by dumping our `acBlocks[Int32]` against a hand-decoded libjxl reference.
3. **Dequant→IDCT bridge factor.** Our `LibjxlIDCT` / `AccelerateDCT` were validated against synthetic coefficient inputs but not against actual dequantised values from a real fixture. A hidden per-axis or per-rank multiplier would only show up under real data.
4. **ZigZag scan-order off-by-one.** `naturalCoeffOrderDCT8` is a verified permutation of 0..63, so this is unlikely but not yet pinned by a hand-crafted-bitstream test.

### Investigation log

The full investigation, including ruled-out suspects, the stage-by-stage diagnostic data, and the ranked next-bite list, lives in [`Documentation/v0.9.0-pixel-accuracy-investigation.md`](v0.9.0-pixel-accuracy-investigation.md) — that's the canonical reference for whoever picks up the next bite.

---

## 6. Risks and remaining work

### Risks

- **Pixel byte-equality timeline.** The v0.9.0 work has been steadily narrowing the suspect space, but the residual quantizer-scaling discrepancy doesn't have a single "drop the ×64 seed" fix. The fix is likely well-defined once the actual mechanism is identified (and may be a one-line change), but the *identification* requires either a libjxl side-by-side trace or a hand-crafted bitstream test.
- **Multi-DC-group support.** Currently throws `notImplemented` for frames spanning multiple DC groups (frames > ~2048 px). Required for full-image medical scans.
- **VarDCT encoder.** Forward path is still pending. The decoder catches up to djxl first, then the encoder builds on the same primitives.
- **Lossless JPEG-1 → JXL reversible transcoding (Phase J).** A distinguishing JPEG XL feature and a stated project requirement; not yet implemented.

### Deferred to follow-on bites

- AFV full overlay (per-AFV-kind corner placement + DC decomposition + IDCT4x4 / 4x8 dispatch). Foundation primitive `AFV.idct4x4` is in place.
- DCT128 and DCT256 strategies (extremely rare in real-world fixtures, only triggered on very large or solid-content frames at low effort).
- Metal compute backend (same `[Float]` UMA contract as `AccelerateDCT`).
- Spec-pixel hand-crafted test fixture (would localise any remaining bug definitively).
- Conformance test vector wiring (`jxl-conformance` repo).

---

## 7. Roadmap forward

| Version | Scope | Demo |
|---|---|---|
| **v0.9.0 (closing)** | Close textured-fixture pixel drift; AFV full overlay; DCT128/256 | Byte-equal to djxl on cjxl-d=1..10 SWEEP fixtures |
| v1.0.0 | Production-ready VarDCT decode; encoder side begins | API stability; package registry release |
| v1.x | VarDCT encoder (matching `cjxl` quality at competitive throughput) | First end-to-end pure-Swift JXL encode/decode |
| v2.x | JPEG-1 ↔ JXL reversible transcoding (Phase J) | Bitwise-identical re-encoded JPEG round-trips |
| v3.x | NEON / Accelerate / Metal optimisation paths land behind feature flags | Performance parity / lead vs libjxl on Apple Silicon |

---

## 8. Repository state

| Artefact | Status |
|---|---|
| `main` branch | Pure-Swift implementation; v0.9.0 in progress |
| `libjxl-backend` branch | Historical reference (Swift wrapper over libjxl) — not a supported runtime path |
| `pre-rewrite-snapshot` branch | Original failed pure-Swift attempt (2025) — preserved for lessons learned |
| Tag `v0.4-libjxl` | Snapshot of the libjxl-wrapped `main` |
| Tag `v0.8.0` | Multi-AC-strategy + UMA backend |

| Reference document | Path |
|---|---|
| Project goals & constraints | [ROADMAP.md](../ROADMAP.md) |
| Public-facing description | [README.md](../README.md) |
| Release history | [CHANGELOG.md](../CHANGELOG.md) |
| Architecture | [Documentation/ARCHITECTURE.md](ARCHITECTURE.md) |
| Session handoff guide | [Documentation/SESSION-NOTES.md](SESSION-NOTES.md) |
| **v0.9.0 pixel-accuracy investigation** | [Documentation/v0.9.0-pixel-accuracy-investigation.md](v0.9.0-pixel-accuracy-investigation.md) |
| Pre-rewrite history (read-only) | [Documentation/legacy/](legacy/) |
| Release checklist for v1.0.0 | [RELEASE_CHECKLIST.md](../RELEASE_CHECKLIST.md) |

---

## 9. Bottom line for management

JXLSwift is on track. The v0.5.0 → v0.8.0 work shipped a fully working VarDCT decoder for every cjxl distance level the conformance corpus exercises, with hardware-accelerated IDCT on Apple Silicon. The current v0.9.0 push to byte-equality is the kind of slow, careful spec-conformance work expected of a production-grade codec — and the recent uniform-block / gradient-block diagnostics have narrowed the residual bug from "vague drift" to "specific quantizer-scaling discrepancy in the AC path with two hard data points characterising it." That's the difference between guesswork and engineering.

No external dependencies, no security exposure surface from third-party C code, full strict-concurrency Swift 6.2 — the architectural posture for DICOMkit integration is intact.

**Recommended next checkpoint:** v0.9.0 byte-equality milestone (textured-fixture max byte-diff ≤ 2 across all SWEEP distances).
